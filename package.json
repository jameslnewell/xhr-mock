{
  "name": "xhr-mock",
  "version": "1.7.0",
  "description": "Utility for mocking XMLHttpRequests in the browser.",
  "keywords": [
    "mock",
    "xhr",
    "test",
    "fake",
    "request",
    "ajax",
    "browser",
    "xmlhttprequest",
    "jquery",
    "superagent"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/jameslnewell/xhr-mock.git"
  },
  "dependencies": {
    "global": "^4.3.0",
    "lie": "^3.1.1"
  },
  "devDependencies": {
    "browserify": "^11.0.1",
    "minifier": "^0.8.1",
    "mochify": "^2.13.0",
    "superagent": "^1.3.0"
  },
  "scripts": {
    "test": "mochify",
    "build": "mkdir -p dist && browserify -r lie -r superagent -r ./index.js:xhr-mock -o dist/xhr-mock.js",
    "minify": "minify --output dist/xhr-mock.min.js dist/xhr-mock.js",
    "postinstall": "npm run build && npm run minify"
  },
  "license": "MIT"
}
