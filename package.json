{
  "name": "xhr-mock-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "packages/*",
      "tests/*",
      "website"
    ],
    "nohoist": [
      "**/@types/**"
    ]
  },
  "dependencies": {
    "@jameslnewell/editor-config": "^1.0.1",
    "@jameslnewell/eslint-config": "^0.12.0",
    "@jameslnewell/jest-preset-lint": "^2.0.0",
    "@jameslnewell/jest-preset-test": "^4.0.0",
    "@jameslnewell/jest-preset-type": "^2.0.0",
    "@jameslnewell/prettier-config": "^1.0.1",
    "@jameslnewell/typescript-config": "^3.0.0",
    "@types/jest": "^24.0.18",
    "eslint": "^6.3.0",
    "husky": "^3.0.5",
    "jest": "^24.9.0",
    "lint-staged": "^9.2.5",
    "prettier": "^1.18.2",
    "typescript": "^3.6.2"
  },
  "scripts": {
    "clean": "yarn workspaces exec lib",
    "lint": "eslint --config .eslintrc.js --ext .ts,.tsx --ignore-pattern node_modules --fix . ",
    "build": "yarn workspaces run build",
    "test": "jest --config .jestrc.js",
    "publish": "yarn run publish"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{json,js,ts,tsx,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
