import {MockRequest, MockResponse} from './types';
export declare function normaliseRequest(req: Partial<MockRequest>): MockRequest;
export declare function normaliseResponse(res: Partial<MockResponse>): MockResponse;
